import {Card, CardContent} from "@mui/material";

const ImageForm = ({data}:{data:any}) => {
    return (
        <Card>
            <CardContent>
                Liste des images...
            </CardContent>
        </Card>
    )
}

export default ImageForm;